function mostrar()
{
	var max = 10;
	var min = 1;

	//var num = Math.floor(Math.random() * ((máximo+1) - mínimo) + mínimo);
	var num = Math.floor(Math.random() * ((max+1) - min) + min);

	alert(num);
	
}//FIN DE LA FUNCIÓN